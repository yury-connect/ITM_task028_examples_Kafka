spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/kafka_demo
    username: postgres
    password: your_password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  kafka:
    bootstrap-servers: localhost:9092



application:
  name: kafka_demo